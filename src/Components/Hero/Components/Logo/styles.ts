import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  gap: 10px;
  align-items: center;
  font-size: 1.4rem;
  color: var(--NEUTRAL);
  z-index: 100;
  font-weight: bold;
`;
